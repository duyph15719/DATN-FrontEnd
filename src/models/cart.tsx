export type CartType = {
    id: string,
    productId: string,
    productImage: string,
    productName: string
    productPrice: number,
    quantity: number
    sizeId: string,
    sizeName: string,
    sizePrice: number,
}